<h1><%= @user.gitname %></h1>

<p>Collaborators:</p>
<ol>
  <% @user.collaborators.each do |col| %>
    <li>
      <%= link_to col.gitname, user_path(col) %>|
      <%= if @user = current_user %>
        <%= simple_for_for [col, current_user.review_for(col)] do |f| %>
          <%= f.input :rating, :collection => 1..5, :as => :radio %>
        <% end %>
    </li>
  <% end %>
</ol>



Repos:

<ol>
  <% @user.repositories.each do |repo| %>
    <li><%= repo.name %></li>
  <% end %>
</ol>

<% if current_user.id == @user.id %>
  <p>Sync Data</p>
<% end %>

